# Build Settings

# Distribution
build.dist=${basedir}/dist

# Export
build.export=${build.dist}/export

# Library
build.lib=${build.dist}/lib

# Logs
build.logs=${build.dist}/logs
build.logs.ant=${build.logs}/ant-build.log
build.logs.svn=${build.logs}/svnCommits.log

# Release
build.release.notes.txt=${basedir}/releaseNotes.txt
build.release.setup.zip=${build.export}/setup.zip
build.release.source.zip=${build.export}/source.zip
build.release.version.txt=${basedir}/version.txt

# Templates
build.templates=${build.dist}/templates

# WAR Archival
build.war=${build.dist}/war
build.war.web.xml=${build.war}/railo.xml

# Wiki
build.wiki.releaseNotes.wiki=${build.wiki.path}/ReleaseNotes.wiki